pwd = input("what is the master password? ")

while True:
    mode = input("what you like to add a new or view existing ones (view,add) ?")
    if mode == "view":
        pass
    elif mode=="add":
        pass
    else:
        print("invalid mode")
        exit()